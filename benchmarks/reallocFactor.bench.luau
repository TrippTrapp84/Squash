--!native
--!optimize 2

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Squash = require(ReplicatedStorage.Squash)

return {
	Functions = {
		Rasterization = function(Profiler)
            
		end
	},
}
